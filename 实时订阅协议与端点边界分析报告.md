# Directus 实时订阅协议与端点边界分析报告

## 目录

1. [默认接入路径配置](#1-默认接入路径配置)
2. [端点可配置关系与环境变量](#2-端点可配置关系与环境变量)
3. [常见端点归类误区](#3-常见端点归类误区)
4. [可操作的排查步骤](#4-可操作的排查步骤)

---

## 1. 默认接入路径配置

### 1.1 总览

Directus 实时订阅功能通过多个独立的 WebSocket 端点提供服务，每个端点有独立的配置和用途。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Directus WebSocket 端点架构                            │
└─────────────────────────────────────────────────────────────────────────────┘

                    ┌──────────────────────────┐
                    │   HTTP Server (upgrade)  │
                    │  监听所有 WebSocket 连接   │
                    └────────────┬─────────────┘
                                 │
            ┌────────────────────┼────────────────────┐
            ▼                    ▼                    ▼
    ┌───────────────┐   ┌───────────────┐   ┌───────────────┐
    │ REST 订阅端点   │   │ GraphQL 订阅  │   │ 日志订阅端点   │
    │  /websocket   │   │   /graphql    │   │ /websocket/logs│
    └───────────────┘   └───────────────┘   └───────────────┘
            │                    │                    │
            ▼                    ▼                    ▼
    ┌───────────────┐   ┌───────────────┐   ┌───────────────┐
    │ 自定义协议     │   │ graphql-ws    │   │ 日志流式推送   │
    │ Directus 专有  │   │ 标准协议      │   │ 系统日志       │
    └───────────────┘   └───────────────┘   └───────────────┘
```

### 1.2 默认路径配置表

| 端点类型 | 配置前缀 | 默认路径 | 协议 | 用途 |
|---------|---------|---------|------|------|
| **REST 订阅** | `WEBSOCKETS_REST_*` | `/websocket` | 自定义 Directus 协议 | 数据集合订阅（items）、协作编辑（collab） |
| **GraphQL 订阅** | `WEBSOCKETS_GRAPHQL_*` | `/graphql` | `graphql-ws` 标准协议 | GraphQL 实时订阅 |
| **日志订阅** | `WEBSOCKETS_LOGS_*` | `/websocket/logs` | 自定义协议 | 系统日志实时推送 |

### 1.3 端点注册机制

**关键点**：所有 WebSocket 控制器共享同一个 HTTP Server 的 `upgrade` 事件。

```typescript
// api/src/server.ts:104-110
if (toBoolean(env['WEBSOCKETS_ENABLED']) === true) {
    createSubscriptionController(server);    // GraphQL
    createWebSocketController(server);        // REST
    createLogsController(server);              // 日志
    startWebSocketHandlers();
}
```

**路径匹配逻辑** (`api/src/websocket/controllers/base.ts:117-119`):

```typescript
protected async handleUpgrade(request: IncomingMessage, socket: internal.Duplex, head: Buffer) {
    const { pathname, query } = parse(request.url!, true);
    if (pathname !== this.endpoint) return;  // 路径不匹配则跳过
    // ... 后续处理
}
```

**重要说明**：
- 每个控制器在 `upgrade` 事件中检查路径
- 如果路径不匹配，控制器立即返回 (`return`)
- 这允许多个控制器监听同一个 HTTP Server
- 但**如果两个控制器配置了相同路径，会产生竞争条件**

---

## 2. 端点可配置关系与环境变量

### 2.1 完整环境变量列表

#### 总开关

| 变量名 | 默认值 | 说明 |
|-------|--------|------|
| `WEBSOCKETS_ENABLED` | `false` | **全局总开关**，必须设为 `true` 才能启用任何 WebSocket 功能 |

#### REST 订阅配置

| 变量名 | 默认值 | 可选值 | 说明 |
|-------|--------|--------|------|
| `WEBSOCKETS_REST_ENABLED` | `true` | `true`/`false` | 是否启用 REST WebSocket 端点 |
| `WEBSOCKETS_REST_PATH` | `/websocket` | 任何有效路径 | REST WebSocket 端点路径 |
| `WEBSOCKETS_REST_AUTH` | `handshake` | `public`/`handshake`/`strict` | 认证模式 |
| `WEBSOCKETS_REST_AUTH_TIMEOUT` | `10` | 数字（秒） | 握手认证超时时间 |
| `WEBSOCKETS_REST_CONN_LIMIT` | 无限制 | 数字 | 最大连接数限制 |

#### GraphQL 订阅配置

| 变量名 | 默认值 | 可选值 | 说明 |
|-------|--------|--------|------|
| `WEBSOCKETS_GRAPHQL_ENABLED` | `true` | `true`/`false` | 是否启用 GraphQL WebSocket 端点 |
| `WEBSOCKETS_GRAPHQL_PATH` | `/graphql` | 任何有效路径 | GraphQL WebSocket 端点路径 |
| `WEBSOCKETS_GRAPHQL_AUTH` | `handshake` | `public`/`handshake`/`strict` | 认证模式 |
| `WEBSOCKETS_GRAPHQL_AUTH_TIMEOUT` | `10` | 数字（秒） | 握手认证超时时间 |
| `WEBSOCKETS_GRAPHQL_CONN_LIMIT` | 无限制 | 数字 | 最大连接数限制 |

#### 日志订阅配置

| 变量名 | 默认值 | 可选值 | 说明 |
|-------|--------|--------|------|
| `WEBSOCKETS_LOGS_ENABLED` | `true` | `true`/`false` | 是否启用日志 WebSocket 端点 |
| `WEBSOCKETS_LOGS_PATH` | `/websocket/logs` | 任何有效路径 | 日志 WebSocket 端点路径 |
| `WEBSOCKETS_LOGS_AUTH` | `handshake` | `public`/`handshake`/`strict` | 认证模式 |
| `WEBSOCKETS_LOGS_AUTH_TIMEOUT` | `10` | 数字（秒） | 握手认证超时时间 |
| `WEBSOCKETS_LOGS_LEVEL` | `info` | `trace`/`debug`/`info`/`warn`/`error`/`fatal` | 日志级别 |
| `WEBSOCKETS_LOGS_STYLE` | `raw` | `raw`/`pretty` | 日志格式 |

#### 其他 WebSocket 配置

| 变量名 | 默认值 | 说明 |
|-------|--------|------|
| `WEBSOCKETS_HEARTBEAT_ENABLED` | `true` | 是否启用心跳检测 |
| `WEBSOCKETS_HEARTBEAT_PERIOD` | `30` | 心跳周期（秒） |
| `WEBSOCKETS_COLLAB_ENABLED` | `true` | 是否启用协作编辑功能（使用 REST 端点） |

### 2.2 认证模式详解

三种认证模式的行为差异：

#### `public` 模式

```
客户端                              服务端
  │                                  │
  │──── 连接 ws://host/path ───────>│
  │                                  │
  │<─── 连接成功 ────────────────────│
  │                                  │
  │ (无认证步骤，直接可用)            │
  │                                  │
```

**适用场景**：公开数据、内部测试环境

#### `handshake` 模式（默认）

```
客户端                              服务端
  │                                  │
  │──── 连接 ws://host/path ───────>│
  │                                  │
  │<─── 连接成功 ────────────────────│
  │                                  │
  │──── { type: 'auth',            │
  │       access_token: '...' } ──>│  (必须在超时前发送)
  │                                  │
  │<─── { type: 'auth',            │
  │       status: 'ok' } ───────────│
  │                                  │
  │ (认证成功，可开始订阅)            │
  │                                  │
```

**适用场景**：生产环境、需要认证的场景

#### `strict` 模式

```
客户端                              服务端
  │                                  │
  │──── 连接 ws://host/path        │
  │      ?access_token=... ───────>│  (token 在 URL 中)
  │                                  │
  │<─── 连接成功或 401 ─────────────│
  │                                  │
  │ (连接时即验证 token，失败则拒绝)  │
  │                                  │
```

**适用场景**：无法在连接后发送消息的环境、安全性要求极高的场景

### 2.3 配置优先级与依赖关系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         配置依赖关系图                                         │
└─────────────────────────────────────────────────────────────────────────────┘

                    WEBSOCKETS_ENABLED = false
                              │
              ┌───────────────┴───────────────┐
              ▼                               ▼
    所有 WebSocket 功能禁用          即使 *_ENABLED = true
    (REST、GraphQL、Logs)             也不会实际启用


                    WEBSOCKETS_ENABLED = true
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
WEBSOCKETS_REST_      WEBSOCKETS_GRAPHQL_    WEBSOCKETS_LOGS_
ENABLED = true        ENABLED = true          ENABLED = true
        │                     │                     │
        ▼                     ▼                     ▼
   监听路径              监听路径               监听路径
   /websocket             /graphql              /websocket/logs
        │                     │                     │
        ▼                     ▼                     ▼
   处理消息类型：         处理消息类型：         处理消息类型：
   - subscribe           - graphql-ws 协议      - subscribe (logs)
   - unsubscribe         - connection_init      - unsubscribe
   - items (CRUD)        - subscribe            - (日志流式推送)
   - collab              - complete
   - ping/pong           - etc.
```

### 2.4 最小配置示例

#### 启用所有 WebSocket 功能

```env
# .env
WEBSOCKETS_ENABLED=true

# REST 订阅（使用默认路径 /websocket）
WEBSOCKETS_REST_ENABLED=true
WEBSOCKETS_REST_AUTH=handshake

# GraphQL 订阅（使用默认路径 /graphql）
WEBSOCKETS_GRAPHQL_ENABLED=true
WEBSOCKETS_GRAPHQL_AUTH=handshake

# 日志订阅（使用默认路径 /websocket/logs）
WEBSOCKETS_LOGS_ENABLED=true
WEBSOCKETS_LOGS_LEVEL=info
```

#### 自定义路径配置

```env
# .env
WEBSOCKETS_ENABLED=true

# 自定义 REST 路径
WEBSOCKETS_REST_PATH=/api/realtime

# 自定义 GraphQL 路径
WEBSOCKETS_GRAPHQL_PATH=/api/graphql-ws

# 自定义日志路径
WEBSOCKETS_LOGS_PATH=/api/logs-stream
```

---

## 3. 常见端点归类误区

### 3.1 误区一："/websocket" 只处理 REST 订阅

**错误认知**：
> "`/websocket` 端点只处理数据集合的订阅消息。"

**实际情况**：

`/websocket` 端点处理**多种消息类型**，包括：

| 消息类型 | type 字段 | 用途 | 处理模块 |
|---------|-----------|------|---------|
| 认证 | `auth` | WebSocket 连接认证 | 基础控制器 |
| 心跳 | `ping`/`pong` | 连接保活 | `heartbeat.ts` |
| 数据订阅 | `subscribe`/`unsubscribe` | 集合变更订阅 | `subscribe.ts` |
| 数据操作 | `items` | 直接通过 WebSocket 进行 CRUD | `items.ts` |
| 协作编辑 | `collab` | 多人实时协作 | `collab/` 目录 |

**证据** (`api/src/websocket/handlers/index.ts:14-18`):

```typescript
const heartbeatEnabled = toBoolean(env['WEBSOCKETS_HEARTBEAT_ENABLED']);
const restEnabled = toBoolean(env['WEBSOCKETS_REST_ENABLED']);
const graphqlEnabled = toBoolean(env['WEBSOCKETS_GRAPHQL_ENABLED']);
const logsEnabled = toBoolean(env['WEBSOCKETS_LOGS_ENABLED']);
const collabEnabled = toBoolean(env['WEBSOCKETS_COLLAB_ENABLED']);
```

**重要**：协作编辑功能（`collab`）**复用** REST WebSocket 端点，不是独立端点。

### 3.2 误区二：GraphQL HTTP 和 WebSocket 使用同一端点

**错误认知**：
> "GraphQL 查询和订阅都使用 `/graphql` 端点，只是协议不同。"

**实际情况**：

| 功能 | 端点 | 协议 | 说明 |
|------|------|------|------|
| GraphQL 查询/突变 | `/graphql` | HTTP POST | 常规 GraphQL 操作 |
| GraphQL 订阅 | **配置的路径** (默认 `/graphql`) | WebSocket | 实时订阅 |
| REST 订阅 | `/websocket` | WebSocket | Directus 自定义协议 |

**关键区别**：

1. **HTTP `/graphql`**：由 Express 路由处理，用于 Query 和 Mutation
2. **WebSocket `/graphql`**：由 `GraphQLSubscriptionController` 处理，用于 Subscription

**潜在问题**：
- 如果反向代理配置不当，可能只转发 HTTP 或只转发 WebSocket
- 需要确保 WebSocket `upgrade` 请求正确到达 Directus

### 3.3 误区三：所有 WebSocket 端点共享配置

**错误认知**：
> "设置 `WEBSOCKETS_AUTH=strict` 会影响所有 WebSocket 端点。"

**实际情况**：

每个端点有**独立的配置前缀**：

```env
# ❌ 这样写是错误的，这些变量不存在
WEBSOCKETS_AUTH=strict
WEBSOCKETS_PATH=/ws

# ✅ 正确的配置方式
WEBSOCKETS_REST_AUTH=strict
WEBSOCKETS_REST_PATH=/ws

WEBSOCKETS_GRAPHQL_AUTH=handshake
WEBSOCKETS_GRAPHQL_PATH=/graphql-ws

WEBSOCKETS_LOGS_AUTH=strict
WEBSOCKETS_LOGS_PATH=/logs
```

**配置代码证据** (`api/src/websocket/controllers/base.ts:67-69`):

```typescript
const endpoint = String(env[`${configPrefix}_PATH`]);
const authMode = AuthMode.safeParse(String(env[`${configPrefix}_AUTH`]).toLowerCase());
const authTimeout = Number(env[`${configPrefix}_AUTH_TIMEOUT`]) * 1000;
```

每个控制器使用不同的 `configPrefix`：
- REST: `WEBSOCKETS_REST`
- GraphQL: `WEBSOCKETS_GRAPHQL`
- Logs: `WEBSOCKETS_LOGS`

### 3.4 误区四：路径配置可以任意相同

**错误认知**：
> "我可以把 REST 和 GraphQL 端点都配置为 `/ws`，这样更简单。"

**实际情况**：

**不建议这样做**，原因：

1. **路径匹配竞争**：
   ```typescript
   // 两个控制器都会监听 upgrade 事件
   // 如果路径相同，无法预测哪个控制器会处理
   
   // REST 控制器检查
   if (pathname !== '/ws') return;  // 匹配
   
   // GraphQL 控制器检查  
   if (pathname !== '/ws') return;  // 也匹配！
   ```

2. **协议不兼容**：
   - REST 端点期望 Directus 自定义协议消息
   - GraphQL 端点期望 `graphql-ws` 标准协议
   - 客户端发送的消息格式不同

3. **子协议协商**：
   - GraphQL 客户端通常发送 `Sec-WebSocket-Protocol: graphql-transport-ws`
   - REST 端点不处理这个子协议

**正确做法**：保持默认路径或使用不同的自定义路径。

### 3.5 误区五：启用 WEBSOCKETS_ENABLED 就够了

**错误认知**：
> "只要设置 `WEBSOCKETS_ENABLED=true`，所有 WebSocket 功能就可用了。"

**实际情况**：

`WEBSOCKETS_ENABLED` 是**总开关**，但各子系统还有独立开关：

```env
# ✅ 正确的完整配置
WEBSOCKETS_ENABLED=true          # 总开关

# 各子系统开关（默认为 true，但显式配置更清晰）
WEBSOCKETS_REST_ENABLED=true     # REST 订阅
WEBSOCKETS_GRAPHQL_ENABLED=true  # GraphQL 订阅
WEBSOCKETS_LOGS_ENABLED=true     # 日志订阅
WEBSOCKETS_COLLAB_ENABLED=true   # 协作编辑（使用 REST 端点）
```

**关键代码** (`api/src/websocket/controllers/index.ts:15-41`):

```typescript
export function createWebSocketController(server: httpServer) {
    const env = useEnv();
    if (toBoolean(env['WEBSOCKETS_REST_ENABLED'])) {  // 独立检查
        websocketController = new WebSocketController(server);
    }
}

export function createSubscriptionController(server: httpServer) {
    const env = useEnv();
    if (toBoolean(env['WEBSOCKETS_GRAPHQL_ENABLED'])) {  // 独立检查
        subscriptionController = new GraphQLSubscriptionController(server);
    }
}
```

**默认值陷阱**：
- `WEBSOCKETS_ENABLED` 默认 `false`
- `WEBSOCKETS_REST_ENABLED` 等默认 `true`
- 这意味着：即使子开关为 true，总开关为 false 时也不会启用

### 3.6 误区六：协作编辑有独立端点

**错误认知**：
> "协作编辑功能有自己的 WebSocket 端点，比如 `/collab`。"

**实际情况**：

协作编辑**复用** REST WebSocket 端点 (`/websocket`)，通过消息 `type` 字段区分：

```typescript
// 协作编辑消息格式
{
    type: 'collab',           // 标识为协作消息
    action: 'join',           // 具体操作
    collection: 'articles',
    item: '123',
    // ...
}
```

**证据** (`api/src/websocket/collab/collab.ts`):

```typescript
// 协作编辑处理逻辑监听 'websocket.message' 事件
// 该事件由 REST WebSocket 控制器触发

emitter.onAction('websocket.message', ({ client, message }) => {
    if (message.type === 'collab') {
        // 处理协作消息
    }
});
```

**配置说明**：
- 协作编辑由 `WEBSOCKETS_COLLAB_ENABLED` 控制
- 但它**不创建**新的端点，只是在 REST 端点上增加消息处理

---

## 4. 可操作的排查步骤

### 4.1 快速验证清单

#### 第一步：检查配置是否正确加载

**操作**：
1. 查看 Directus 启动日志
2. 确认 WebSocket 相关的 info 日志

**期望日志**：
```
INFO: WebSocket Server started at http://0.0.0.0:8055/websocket
INFO: GraphQL Subscriptions started at http://0.0.0.0:8055/graphql
```

**异常情况**：
- 没有这些日志 → `WEBSOCKETS_ENABLED` 可能为 false
- 路径与预期不符 → 检查 `*_PATH` 配置

#### 第二步：验证端点可访问性

**使用 wscat 工具测试**：

```bash
# 安装 wscat
npm install -g wscat

# 测试 REST WebSocket 端点
wscat -c ws://localhost:8055/websocket

# 测试 GraphQL WebSocket 端点
wscat -c ws://localhost:8055/graphql -s graphql-transport-ws

# 测试带 token 的连接 (strict 模式)
wscat -c "ws://localhost:8055/websocket?access_token=your-token"
```

**预期结果**：
- 连接成功建立（wscat 显示 `Connected`）
- 如果是 `handshake` 模式，需要在超时前发送认证消息

**常见错误**：

| 错误现象 | 可能原因 | 排查方向 |
|---------|---------|---------|
| 连接立即关闭 | 认证失败 | 检查 token 有效性、认证模式配置 |
| 连接超时 | 网络问题、反向代理 | 检查防火墙、Nginx 配置 |
| 404 响应 | 路径错误或端点未启用 | 检查 `*_PATH` 和 `*_ENABLED` |
| 403 响应 | 权限不足 | 检查用户权限、连接数限制 |

#### 第三步：发送测试消息

**REST 订阅测试**：

```bash
# 连接后，发送认证消息
> {"type":"auth","access_token":"your-token"}

# 期望响应
< {"type":"auth","status":"ok"}

# 发送订阅消息
> {"type":"subscribe","collection":"articles","uid":"test-1"}

# 期望响应（初始数据）
< {"type":"subscription","uid":"test-1","event":"init","data":[...]}
```

**GraphQL 订阅测试**：

```bash
# 使用 graphql-ws 协议
wscat -c ws://localhost:8055/graphql -s graphql-transport-ws

# 发送 connection_init
> {"type":"connection_init","payload":{"access_token":"your-token"}}

# 期望响应
< {"type":"connection_ack"}

# 发送订阅
> {"id":"1","type":"subscribe","payload":{"query":"subscription { articles_mutated { key event data { id title } } }"}}

# 期望响应（当有数据变更时）
< {"id":"1","type":"next","payload":{"data":{"articles_mutated":{"key":"123","event":"create","data":{...}}}}}
```

### 4.2 分步排查指南

#### 阶段 1：环境与配置检查

```bash
# 1. 检查 Directus 版本和 Node.js 版本
node --version
# 应 >= 22

# 2. 检查环境变量配置
# 在 Directus 运行环境中执行：
console.log('WEBSOCKETS_ENABLED:', process.env.WEBSOCKETS_ENABLED);
console.log('WEBSOCKETS_REST_PATH:', process.env.WEBSOCKETS_REST_PATH);
console.log('WEBSOCKETS_GRAPHQL_PATH:', process.env.WEBSOCKETS_GRAPHQL_PATH);

# 3. 检查端口监听
netstat -an | grep 8055
# 或
lsof -i :8055
```

#### 阶段 2：网络与反向代理检查

**如果使用 Nginx 反向代理**：

```nginx
# 确保 WebSocket 升级配置正确
location /websocket {
    proxy_pass http://directus:8055;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_read_timeout 86400;  # 长连接超时
}

location /graphql {
    # 注意：需要同时支持 HTTP 和 WebSocket
    proxy_pass http://directus:8055;
    
    # WebSocket 特定配置
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    
    # 其他代理配置
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

# Nginx 1.3.13+ 支持的连接升级变量
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}
```

**检查要点**：
1. `proxy_http_version 1.1` 必须设置
2. `Upgrade` 和 `Connection` 头必须正确转发
3. `proxy_read_timeout` 应该足够长（默认 60s 可能导致心跳断开）

#### 阶段 3：认证与权限检查

**认证模式验证矩阵**：

| 模式 | URL 参数 | 连接后消息 | 预期行为 |
|------|----------|-----------|---------|
| `public` | 不需要 | 不需要 | 连接即成功 |
| `handshake` | 可选 | 必须发送 `auth` | 超时未认证则断开 |
| `strict` | **必须** `access_token` | 不需要 | URL 无 token 则 401 |

**测试认证模式**：

```bash
# public 模式测试
wscat -c ws://localhost:8055/websocket
# 预期：连接保持，无需认证

# handshake 模式测试
wscat -c ws://localhost:8055/websocket
# 10秒内不发送认证，连接会断开
# 发送：> {"type":"auth","access_token":"valid-token"}
# 接收：< {"type":"auth","status":"ok"}

# strict 模式测试
wscat -c ws://localhost:8055/websocket
# 预期：立即 401 关闭

wscat -c "ws://localhost:8055/websocket?access_token=valid-token"
# 预期：连接成功
```

#### 阶段 4：订阅功能验证

**REST 订阅完整测试流程**：

```typescript
// 使用 SDK 测试
import { createDirectus, realtime, authentication, rest } from '@directus/sdk';

const client = createDirectus('http://localhost:8055')
    .with(authentication('json'))
    .with(realtime({ authMode: 'handshake' }))
    .with(rest());

// 1. 登录
await client.login('admin@example.com', 'password');

// 2. 连接 WebSocket
await client.connect();
console.log('Connected');

// 3. 订阅
const { subscription, unsubscribe } = await client.subscribe('articles', {
    query: { fields: ['id', 'title', 'status'] }
});

console.log('Subscribed, waiting for events...');

// 4. 监听事件
(async () => {
    for await (const event of subscription) {
        console.log('Event received:', event);
        // 此时在另一个终端修改 articles 表数据
        // 应该能看到 create/update/delete 事件
    }
})();

// 5. 测试完成后取消订阅
// setTimeout(unsubscribe, 60000);
```

**GraphQL 订阅完整测试流程**：

```typescript
import { createClient } from 'graphql-ws';
import WebSocket from 'ws';

const client = createClient({
    url: 'ws://localhost:8055/graphql',
    webSocketImpl: WebSocket,
    connectionParams: {
        access_token: 'your-access-token'
    }
});

// 订阅
const unsubscribe = client.subscribe(
    {
        query: `
            subscription {
                articles_mutated {
                    key
                    event
                    data {
                        id
                        title
                        status
                    }
                }
            }
        `
    },
    {
        next: (data) => {
            console.log('Subscription data:', data);
        },
        error: (error) => {
            console.error('Subscription error:', error);
        },
        complete: () => {
            console.log('Subscription completed');
        }
    }
);

// 测试完成后
// unsubscribe();
```

### 4.3 常见问题诊断表

| 问题现象 | 可能原因 | 诊断步骤 | 解决方案 |
|---------|---------|---------|---------|
| **连接立即断开** | 1. 认证模式配置错误<br>2. Token 无效<br>3. 连接数超限 | 1. 检查 `*_AUTH` 配置<br>2. 验证 token 有效性<br>3. 检查 `*_CONN_LIMIT` | 1. 确认认证模式与客户端匹配<br>2. 使用有效 token<br>3. 调整连接限制 |
| **连接成功但无消息** | 1. 订阅消息格式错误<br>2. 权限不足<br>3. 集合不存在 | 1. 检查订阅消息 `type` 和 `collection`<br>2. 验证用户权限<br>3. 确认集合名称 | 1. 修正消息格式<br>2. 分配正确权限<br>3. 使用正确的集合名 |
| **GraphQL 连接失败** | 1. 子协议未指定<br>2. 路径配置错误 | 1. 检查是否使用 `graphql-transport-ws`<br>2. 确认 `WEBSOCKETS_GRAPHQL_PATH` | 1. 指定正确的子协议<br>2. 修正路径配置 |
| **订阅后无变更事件** | 1. 事件类型过滤<br>2. 特定项目订阅<br>3. 权限过滤后无数据 | 1. 检查是否设置了 `event` 参数<br>2. 检查是否设置了 `item` 参数<br>3. 验证数据权限 | 1. 移除不必要的过滤<br>2. 确保有访问权限 |
| **Nginx 后连接不稳定** | 1. `proxy_read_timeout` 太短<br>2. 未正确转发 Upgrade 头 | 1. 检查 Nginx 超时配置<br>2. 验证 Upgrade 和 Connection 头 | 1. 增加 `proxy_read_timeout`<br>2. 修正 Nginx 配置 |
| **断开重连后订阅丢失** | 1. 客户端未实现重连逻辑<br>2. 服务端清理了订阅 | 1. 检查客户端重连处理<br>2. 确认是否需要重新订阅 | 1. 实现重连后自动重订<br>2. 使用 SDK 的自动重连功能 |

### 4.4 快速验证脚本

创建一个测试脚本 `test-websocket.js`：

```javascript
import { createDirectus, realtime, authentication, rest } from '@directus/sdk';

async function testWebSocket() {
    console.log('=== Directus WebSocket 测试 ===\n');
    
    // 配置
    const url = process.env.DIRECTUS_URL || 'http://localhost:8055';
    const email = process.env.DIRECTUS_EMAIL || 'admin@example.com';
    const password = process.env.DIRECTUS_PASSWORD || 'password';
    const testCollection = process.env.TEST_COLLECTION || 'articles';
    
    console.log(`配置信息:`);
    console.log(`  URL: ${url}`);
    console.log(`  邮箱: ${email}`);
    console.log(`  测试集合: ${testCollection}\n`);
    
    // 创建客户端
    const client = createDirectus(url)
        .with(authentication('json'))
        .with(realtime({ 
            authMode: 'handshake',
            debug: true  // 开启调试日志
        }))
        .with(rest());
    
    try {
        // 阶段 1: 登录测试
        console.log('[1/5] 测试登录...');
        await client.login(email, password);
        console.log('      ✓ 登录成功\n');
        
        // 阶段 2: WebSocket 连接测试
        console.log('[2/5] 测试 WebSocket 连接...');
        await client.connect();
        console.log('      ✓ WebSocket 连接成功\n');
        
        // 阶段 3: 订阅测试
        console.log(`[3/5] 测试订阅集合: ${testCollection}...`);
        const { subscription, unsubscribe } = await client.subscribe(testCollection, {
            query: { limit: 5 }
        });
        console.log('      ✓ 订阅成功\n');
        
        // 阶段 4: 测试取消订阅
        console.log('[4/5] 测试取消订阅...');
        
        // 监听一小段时间
        const timeout = setTimeout(() => {
            console.log('      等待事件超时（无数据变更属于正常）');
        }, 3000);
        
        // 简单的事件监听
        const listener = (async () => {
            for await (const event of subscription) {
                console.log(`      收到事件: ${event.event}`);
                clearTimeout(timeout);
                break;  // 收到一个事件就结束
            }
        })();
        
        await Promise.race([listener, new Promise(r => setTimeout(r, 3500))]);
        
        // 取消订阅
        unsubscribe();
        console.log('      ✓ 取消订阅成功\n');
        
        // 阶段 5: 断开连接
        console.log('[5/5] 测试断开连接...');
        client.disconnect();
        console.log('      ✓ 断开连接成功\n');
        
        console.log('=== 所有测试完成 ===');
        console.log('\n总结:');
        console.log('  ✓ 登录功能正常');
        console.log('  ✓ WebSocket 连接正常');
        console.log('  ✓ 订阅功能正常');
        console.log('  ✓ 取消订阅功能正常');
        
    } catch (error) {
        console.error('\n❌ 测试失败:', error.message);
        console.error('\n排查建议:');
        console.error('  1. 检查 Directus 是否运行');
        console.error('  2. 确认 URL、邮箱、密码配置正确');
        console.error('  3. 检查测试集合是否存在');
        console.error('  4. 验证 WEBSOCKETS_ENABLED=true');
        process.exit(1);
    }
}

testWebSocket();
```

**使用方法**：

```bash
# 设置环境变量
export DIRECTUS_URL=http://localhost:8055
export DIRECTUS_EMAIL=admin@example.com
export DIRECTUS_PASSWORD=your-password
export TEST_COLLECTION=articles

# 运行测试
node test-websocket.js
```

---

## 参考文件

| 文件路径 | 说明 |
|---------|------|
| `api/src/websocket/controllers/base.ts` | WebSocket 控制器基类（路径匹配、认证） |
| `api/src/websocket/controllers/rest.ts` | REST WebSocket 控制器 |
| `api/src/websocket/controllers/graphql.ts` | GraphQL WebSocket 控制器 |
| `api/src/websocket/controllers/logs.ts` | 日志 WebSocket 控制器 |
| `api/src/websocket/controllers/index.ts` | 控制器注册入口 |
| `api/src/server.ts` | 服务器初始化（WebSocket 注册） |
| `packages/env/src/constants/defaults.ts` | 默认环境变量配置 |
